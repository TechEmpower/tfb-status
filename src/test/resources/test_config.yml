http:
  host: "localhost"
  port: 8443
  keyStore:
    path: localhost.jks
    password: password

email:
  host: localhost
  port: 8025
  from: tfb+testing@techempower.com
  to: tfb+testing@techempower.com

assets:
  mode: CLASS_PATH
  root: assets

mustache:
  mode: CLASS_PATH
  root: mustache

fileStore:
  root: test_managed_files
