http:
  host: "localhost"
  port: 8443
  keyStore:
    path: localhost.jks
    password: password

email:
  host: localhost
  port: 8025
  from: tfb+testing@techempower.com
  to: tfb+testing@techempower.com

assets:
  mode: CLASS_PATH

mustache:
  mode: CLASS_PATH

fileStore:
  root: test_managed_files

runProgressMonitor:
  environmentTimeoutSeconds: 1
  maxEnvironments: 100

runCompleteMailer:
  minSecondsBetweenEmails: 0

sharing:
  minSecondsBetweenEmails: 0
  maxDirectorySizeInBytes: 10000000
  maxFileSizeInBytes: 5000000
  tfbStatusOrigin: https://tfb-status.localhost
  tfbWebsiteOrigin: https://tfb-website.localhost
